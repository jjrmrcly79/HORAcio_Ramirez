.env
.env.*
*.local
node_modules/
.DS_Store
/tmp/
# Los secretos (BOT_TOKEN, SERVICE_ROLE_KEY) viven en el nodo n8n, nunca en el repo.

scripts/secrets.env
.claude/settings.local.json

# Datos sensibles de personal (perfiles ADKAR + demografía) — NUNCA al repo público.
# Viven en disco solo para importarlos a Supabase (RH-only); fuera de git.
Organigrama/

# Graphify — cache local y backups por fecha (regenerables). El grafo sí se commitea.
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
